{
  "name": "coda-ccw-proxy",
  "version": "1.0.0",
  "description": "Local proxy server that fetches CCW course data from Coda directly — no model in the loop",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
